# WordPress Design Core Hub v0.2.1

Canvas rendering update: Design Core managed pages bypass the active theme through `templates/canvas.php`, preventing theme headers, footers, post titles, content widths, block gaps, and typography from corrupting reference-derived layouts.
